# Merge this hostname into /etc/cloudflared/config.yml (do not replace
# existing hostnames). 8000/8001 are already in use — Teoria listens on 8012.
#
#   sudo systemctl restart cloudflared
#
# Point DNS (once per tunnel):
#   sudo cloudflared tunnel route dns <TUNNEL_NAME> teoria.g1orga.dev

tunnel: YOUR_TUNNEL_ID
credentials-file: /etc/cloudflared/YOUR_TUNNEL_ID.json

ingress:
  - hostname: teoria.g1orga.dev
    service: http://127.0.0.1:8012
  # keep your other hostnames here
  - service: http_status:404
